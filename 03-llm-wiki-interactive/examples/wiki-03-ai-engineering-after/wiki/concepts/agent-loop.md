---
type: concept
title: Agent Loop
description: The boundary-yielding turn loop of a coding agent's interactive TUI — a ReAct-style plan/explore/apply/execute/observe cycle with no hard-coded step cap, steerable only at two defined checkpoints, and structurally distinct from the headless path.
aliases: []
sources:
  - "[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/sources/article-the-coding-agent-loop]]"
  - "[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]"
  - "[[tool-call-routing-to-the-permission-gate]]"
  - "[[how-a-subagent-is-spawned-and-what-the-parent-gets-back]]"
related:
  - "[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/agent-harness]]"
  - "[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/permission-gate]]"
  - "[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/context-compaction]]"
  - "[[subagents]]"
created: 2026-08-29T17:08:57Z
timestamp: 2026-08-31T14:48:40Z
source_count: 4
---

# Agent Loop

> One turn is not one model call — it's a generator that yields at two checkpoints and otherwise loops until the model itself signals it's done. That generator only runs in the interactive TUI; the headless path bypasses it entirely.

## Definition

All four source-like pages describe the same `decode` codebase's turn loop, at increasing zoom levels. The article frames it conceptually as a ReAct cycle — plan (`todo_write`) → explore (`read`/`glob`/`grep`) → apply (`write`/`edit`, gated behind approval) → execute (`bash`) → observe (exit code or output feeds the next pass) — implemented as an `AgentTurnHandler` async generator chaining `agent.iter` steps, exposing exactly two yield points, `Boundary.MODEL_REQUEST` and `Boundary.WOULD_STOP`, with **no max-step cap**: the loop trusts the model's own text-instead-of-tool-call signal that it is finished. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/sources/article-the-coding-agent-loop]] The repo's architecture page names the same subsystem "the subsystem that makes the codebase what it is" and adds the mechanics underneath it: a `harness.Runner` owns a single-flight phase machine, drives the handler by sending it whatever it drained at each boundary, and the handler in turn talks to a `PermissionGate` and heals a broken history. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]] A third, narrower repo note traces one path through that machinery in detail — a gated tool call to `gate.check()` — and in doing so sharpens where the loop actually *suspends*: the Runner's turn task parks mid-frame inside `await agen.asend(...)` while a permission ASK is pending, not at either yielded `Boundary`; the two boundaries are where steering/follow-up/abort are checked, but a human-approval wait happens deeper inside a single leg. The same note finds that the boundary/queue machinery described above is **TUI-only** — headless `run_agent_task_hitl` never constructs an `AgentTurnHandler` or `Runner` at all, calling `KitaruAgent(...).run_sync(...)` directly instead, so `agent/loop.py`'s generator, its two boundaries, and the steering/follow-up queues play no part in a headless run. [[tool-call-routing-to-the-permission-gate]] A fourth, narrower repo note tracing subagent fan-out sharpens what "the agent loop" even refers to in this codebase: a subagent is not a second loop implementation running in parallel — `_run_attempt` re-enters the *same installed* `Agent` object (the one `set_main_agent()` fixed at `build_agent()` time) via a plain nested `Agent.run()` call, sharing the parent's model and HTTP client, scoped only by a freshly built, narrowed `AgentDeps` for that one call. So "the loop" in `decode` is one `Agent` instance, called recursively with different deps, not N independent loop instances. [[how-a-subagent-is-spawned-and-what-the-parent-gets-back]]

> Synthesis: all four source-like pages trace to the same author, project and codebase (Paul Iusztin's `decode` course) — this is one voice describing one implementation at increasing depth, not four independent confirmations. The article is the dedicated deep-dive on the loop's *rationale* (ReAct framing, the no-step-cap philosophy, the Pi comparison); the architecture page sketches its *mechanics* as one of several subsystems (phase machine, deferred tool requests, history healing); the permission-routing note traces one call path through it to reveal that the boundary/queue loop is TUI-specific; the subagent-spawn note traces a second path through it to reveal that "the loop" is a single `Agent` object re-entered recursively, not a family of loop implementations. Read as four passes over the same voice, not as corroboration.

## Key claims

- The turn lifecycle is plan → explore → apply → execute → observe, with no hard-coded step cap by design — a step cap is "a guess," and the signal worth watching instead is the context window. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/sources/article-the-coding-agent-loop]]
- The handler is an async generator exposing exactly two yield points — `Boundary.MODEL_REQUEST` for steering, `Boundary.WOULD_STOP` for follow-up — confirmed at the code level by a `Runner`/`AgentTurnHandler`/`agent.iter` sequence diagram. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/sources/article-the-coding-agent-loop]], [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- Steering (plain Enter) folds into the prompt at the next `MODEL_REQUEST`; follow-up (Alt+Enter) is drained only at `WOULD_STOP` and buys one more leg — because injecting text mid-tool-call would corrupt the running turn. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/sources/article-the-coding-agent-loop]], [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- A gated tool call doesn't block the loop — it returns a `DeferredToolRequests`, which the handler routes to the permission gate and resumes the same leg with `DeferredToolResults` once resolved; this is what makes both approval prompts and mid-turn steering possible. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- The abort flag is checked only at the two boundaries, never mid-stream or mid-tool, so an Esc-abort keeps the history produced so far; a racing second submission is caught because the runner's phase flips to "busy" synchronously before its first `await`. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- The handler heals history whose last message carries tool calls with no results — from a crashed or aborted leg — since an unhealed gap would make the underlying framework reject every later prompt and brick the session. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- Where a permission ASK actually parks: not at a yielded `Boundary`, but mid-frame inside the Runner's own turn task, at `await agen.asend(sent)` — `runner.phase` stays `RUNNING` the entire wait, and the two boundaries checked for abort/steering are not where this suspension lives. [[tool-call-routing-to-the-permission-gate]]
- The headless `--hitl` path never builds an `AgentTurnHandler` or `Runner` at all — it calls `KitaruAgent(...).run_sync(...)` directly — so the boundary-yielding generator, its two checkpoints, and the steering/follow-up queues described above are a TUI-only mechanism; headless runs suspend instead on Kitaru durable flow-scope waits or the adapter's own approval checkpoint. [[tool-call-routing-to-the-permission-gate]]
- A subagent is not a second loop instance: `_run_attempt` re-enters the *same* installed `Agent` object via a plain nested `Agent.run()` call — sharing the parent's model and HTTP client — with only a fresh, narrowed `AgentDeps` (bypass-mode gate, deny resolvers, `active_agent=explore`) distinguishing the child's call from the parent's; "the agent loop" in this codebase is one `Agent`, invoked recursively with different deps, not a family of independent loop implementations. [[how-a-subagent-is-spawned-and-what-the-parent-gets-back]]

## Relationships

- **[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/agent-harness]]**: the loop is the thin core the harness wraps — construction is short by design; everything that makes the agent good (tools, gate, sandbox, memory, skills, compaction) sits outside it. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- **[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/permission-gate]]**: the loop never blocks on approval itself — it defers via `DeferredToolRequests` and lets the gate decide, then resumes; in the TUI that resume is driven by an in-process `asyncio.Future`, while headless `--hitl` routes around the loop's own generator entirely. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]], [[tool-call-routing-to-the-permission-gate]]
- **[[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/concepts/context-compaction]]**: compaction is checked at the same `WOULD_STOP` boundary the loop already exposes, rather than adding a separate checkpoint. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]]
- **[[subagents]]**: subagent fan-out is not a separate execution path but a nested call on the *same* `Agent` object — the child re-enters `Agent.run()` directly, bypassing decode's own harness loop (`agent/loop.py` never runs for a child), scoped only by a narrowed `AgentDeps`. [[03-llm-wiki-interactive/examples/wiki-ai-engineering-after/wiki/repos/github-decodingai-magazine-building-a-coding-agent-from-scratch-course/ARCHITECTURE]], [[how-a-subagent-is-spawned-and-what-the-parent-gets-back]]

> Synthesis: four passes at increasing depth over one implementation — the loop's headline shape (yield at two boundaries, no step cap) holds up under closer reading, but that closer reading also shows the shape is scoped to the interactive TUI (not headless runs) and to the top-level turn (not a subagent's own `run()`, which shares the same `Agent` but skips this harness loop entirely).
